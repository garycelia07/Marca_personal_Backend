# Dependencias
node_modules

# Este proyecto usa pnpm — ignorar lockfiles de otros gestores para que nadie los commitee por error
package-lock.json
yarn.lock

# Build
dist
*.tsbuildinfo

# Variables de entorno (contienen credenciales de Supabase, JWT_SECRET, etc.)
.env
.env.local
.env.*.local
.env.test

# Archivos subidos al VPS (PDFs/imágenes de materiales) — no van al repo
uploads/

# Logs
*.log
npm-debug.log*

# Tests
coverage

# Editor / OS
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db
